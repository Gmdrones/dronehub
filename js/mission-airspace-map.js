(function(){
  var sarpasUrl='https://servicos.decea.mil.br/sarpas/';
  function ready(fn){document.readyState==='loading'?document.addEventListener('DOMContentLoaded',fn):fn();}
  function esc(v){return String(v||'').replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
  function missions(){try{return typeof getMissoes==='function'?getMissoes():[];}catch(e){return [];}}
  function save(list){if(typeof saveMissoes==='function')saveMissoes(list);}
  function isMissionPage(){return location.pathname.indexOf('missoes')>-1;}
  function airspacePanel(){if(!isMissionPage()||!window.isPro||document.getElementById('airspacePanel'))return;var main=document.querySelector('.main');var card=main&&main.querySelector('.card');if(!card)return;var panel=document.createElement('section');panel.id='airspacePanel';panel.className='airspace-panel';panel.innerHTML='<div><span class="airspace-panel__eyebrow">ESPAÇO AÉREO E SARPAS</span><h3>Planeje legalmente antes de decolar.</h3><p>Vincule o protocolo SARPAS à missão e abra a consulta oficial do DECEA. O Drone Hub não confirma autorização de voo automaticamente.</p><a target="_blank" rel="noopener" href="'+sarpasUrl+'">Abrir SARPAS oficial ↗</a></div><div class="airspace-panel__check"><b>Confirmação oficial obrigatória</b><span>Consulte no SARPAS possíveis restrições, aeródromos, áreas condicionadas e autorizações aplicáveis ao local e à data da operação.</span></div>';card.insertAdjacentElement('afterend',panel);}
  function sarpasField(){if(!isMissionPage()||!window.isPro||document.getElementById('mSarpasStatus'))return;var grid=document.querySelector('.main .card .grid-2');if(!grid)return;var old=document.getElementById('mSarpas');if(old){var wrap=document.createElement('div');wrap.innerHTML='<label for="mSarpasStatus">Situação da consulta oficial</label><select id="mSarpasStatus"><option value="pendente">Consulta SARPAS pendente</option><option value="solicitada">Solicitação enviada</option><option value="autorizacao_informada">Código/autorização informado</option></select>';grid.appendChild(wrap);return;}var field=document.createElement('div');field.innerHTML='<label for="mSarpas">Código SARPAS / autorização</label><input id="mSarpas" type="text" placeholder="Informe o código após a consulta oficial">';grid.appendChild(field);var status=document.createElement('div');status.innerHTML='<label for="mSarpasStatus">Situação da consulta oficial</label><select id="mSarpasStatus"><option value="pendente">Consulta SARPAS pendente</option><option value="solicitada">Solicitação enviada</option><option value="autorizacao_informada">Código/autorização informado</option></select>';grid.appendChild(status);var create=window.criarMissao;if(typeof create==='function'){window.criarMissao=function(){var code=document.getElementById('mSarpas').value.trim(),statusValue=document.getElementById('mSarpasStatus').value;create.apply(this,arguments);var list=missions();if(!list.length)return;var latest=list.slice().sort(function(a,b){return String(b.createdAt||'').localeCompare(String(a.createdAt||''));})[0];if(latest){latest.sarpas=code;latest.sarpasStatus=statusValue;save(list);}document.getElementById('mSarpas').value='';document.getElementById('mSarpasStatus').value='pendente';};}}
  function cache(){try{return JSON.parse(localStorage.getItem('dronehub_geocode_cache')||'{}');}catch(e){return {};}}
  function geo(address){var c=cache();if(c[address])return Promise.resolve(c[address]);return fetch('https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&q='+encodeURIComponent(address),{headers:{Accept:'application/json'}}).then(function(r){return r.json();}).then(function(rows){if(!rows[0])return null;var point={lat:Number(rows[0].lat),lon:Number(rows[0].lon),label:rows[0].display_name};c[address]=point;localStorage.setItem('dronehub_geocode_cache',JSON.stringify(c));return point;}).catch(function(){return null;});}
  function mapCard(){if(!window.isPro||document.getElementById('missionMapCard'))return;var main=document.querySelector('.main');if(!main)return;var card=document.createElement('section');card.id='missionMapCard';card.className='card mission-map-card';card.innerHTML='<div class="mission-map-card__head"><div><h3>Mapa de operações</h3><p>Endereços das próximas missões, com leitura visual do planejamento.</p></div><div class="mission-map-badge"><i></i>DADOS DA MISSÃO</div></div><div id="missionMap" class="mission-map"></div>';var anchor=document.getElementById('dashboardCommand')||document.getElementById('missionTools')||main.querySelector('.tabs')||main.querySelector('.card');anchor.insertAdjacentElement('afterend',card);renderMap();}
  function renderMap(){var target=document.getElementById('missionMap');if(!target)return;var list=missions().filter(function(m){return m.local&&m.status!=='concluida';}).slice(0,8);if(!list.length){target.className='mission-map-empty';target.textContent='Cadastre uma missão com endereço para visualizar os locais de operação neste mapa.';return;}if(!window.L){target.className='mission-map-empty';target.textContent='O mapa não pôde ser carregado agora. Verifique sua conexão e tente novamente.';return;}var map=L.map(target,{zoomControl:false,attributionControl:true}).setView([-14.235,-51.9253],4);L.control.zoom({position:'topright'}).addTo(map);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(map);Promise.all(list.map(function(m){return geo(m.local).then(function(point){return {mission:m,point:point};});})).then(function(items){var bounds=[];items.forEach(function(item){if(!item.point)return;var icon=L.divIcon({className:'',html:'<div class="ops-map-marker"><span>⌖</span></div>',iconSize:[30,30],iconAnchor:[15,30]});var m=item.mission;var detail='<b>'+esc(m.titulo||'Missão')+'</b><small>'+esc(m.local)+'</small><br><small>'+esc(m.data||'Data a definir')+' · '+esc(m.horario||'')+'</small><br><small>SARPAS: '+(m.sarpas?'código informado':'consulta pendente')+'</small>';L.marker([item.point.lat,item.point.lon],{icon:icon}).addTo(map).bindPopup(detail);bounds.push([item.point.lat,item.point.lon]);});if(bounds.length)map.fitBounds(bounds,{padding:[38,38],maxZoom:13});else target.className='mission-map-empty',target.textContent='Não foi possível localizar os endereços cadastrados. Revise o endereço da missão e tente novamente.';});}
  ready(function(){airspacePanel();sarpasField();mapCard();});
}());
